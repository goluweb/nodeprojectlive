{
    "builds": [
      { "src": "package.json", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.js" }
    ],
    "scripts": {
      "build": "npm install && npm run build",
      "start": "node index.js"
    }
  }
  